@using ByTescaro.ConstrutorApp.Application.DTOs
@using ByTescaro.ConstrutorApp.Application.Interfaces
@using ByTescaro.ConstrutorApp.UI.Authentication
@using ByTescaro.ConstrutorApp.UI.Services
@using Microsoft.AspNetCore.Components.Authorization
@implements IDisposable
@inject NavigationManager NavigationManager
@inject IUsuarioLogadoService UsuarioLogadoService
@inject AuthenticationStateProvider AuthProvider
@inject IJSRuntime JSRuntime
@inject AuthCookieService AuthService
@inject PersonalizacaoApiService PersonalizacaoApiService

<AuthorizeView>
    <Authorized>
        <RadzenPanelMenu Style="width: 100%;">
            <RadzenPanelMenuItem Text=@($"Olá {nomeUsuario}") Disabled="true" />
            <RadzenPanelMenuItem Text="Dashboard" Icon="home" Path="" />

            <AuthorizeView Roles="Administrador,SA" Context="adminContextGP">
                <Authorized>
                    <RadzenPanelMenuItem Text="Projetos" Icon="construction">
                        <RadzenPanelMenuItem Text="Projetos" Icon="business" Path="projetos" />

                        <RadzenPanelMenuItem Text="Orçamentos" Icon="paid" Path="orcamentos" />
                    </RadzenPanelMenuItem>
                </Authorized>
            </AuthorizeView>

            <AuthorizeView Roles="Administrador,SA" Context="adminContextCG">
                <Authorized>
                    <RadzenPanelMenuItem Text="Cadastros" Icon="folder_shared">
                        <RadzenPanelMenuItem Text="Clientes" Icon="people" Path="clientes" />
                        <RadzenPanelMenuItem Text="Etapas da Obra" Icon="format_list_numbered" Path="obraetapaspadrao" />
                        <RadzenPanelMenuItem Text="Equipamentos" Icon="build" Path="equipamentos" />
                        <RadzenPanelMenuItem Text="Fornecedores" Icon="contacts_product" Path="fornecedores" />
                        <RadzenPanelMenuItem Text="Funções" Icon="badge" Path="funcoes" />
                        <RadzenPanelMenuItem Text="Funcionários" Icon="work" Path="funcionarios" />
                        <RadzenPanelMenuItem Text="Insumos" Icon="inventory" Path="insumos" />
                        <RadzenPanelMenuItem Text="Itens da Etapa" Icon="checklist" Path="obraitensetapapadrao" />
                        <RadzenPanelMenuItem Text="Serviços" Icon="design_services" Path="servicos" />
                    </RadzenPanelMenuItem>
                </Authorized>
            </AuthorizeView>

            <AuthorizeView Roles="Administrador,SA" Context="adminContextImport">
                <Authorized>
                    <RadzenPanelMenuItem Text="Importações" Icon="upload">
                        <RadzenPanelMenuItem Text="Clientes" Path="/importar/clientes" Icon="people" />
                        <RadzenPanelMenuItem Text="Equipamentos" Path="/importar/equipamentos" Icon="build" />
                        <RadzenPanelMenuItem Text="Fornecedores" Path="/importar/fornecedores" Icon="contacts_product" />
                        <RadzenPanelMenuItem Text="Insumos" Path="/importar/insumos" Icon="inventory" />
                        <RadzenPanelMenuItem Text="Serviços" Path="/importar/servicos" Icon="design_services" />
                    </RadzenPanelMenuItem>
                </Authorized>
            </AuthorizeView>

            <AuthorizeView Roles="Administrador,SA" Context="adminContextConfig">
                <Authorized>
                    <RadzenPanelMenuItem Text="Configurações" Icon="settings">
                        <RadzenPanelMenuItem Text="Lembretes Diários" Icon="alarm" Path="configuracoes/lembretesdiarios" />
                        <RadzenPanelMenuItem Text="Personalização" Icon="palette" Path="personalizacao" />
                    </RadzenPanelMenuItem>
                </Authorized>
            </AuthorizeView>

            <AuthorizeView Roles="Administrador,SA" Context="adminContextAdmin">
                <Authorized>
                    <RadzenPanelMenuItem Text="Administração" Icon="admin_panel_settings">
                        <RadzenPanelMenuItem Text="Perfis de Usuários" Icon="list" Path="perfilUsuarios" />
                        <RadzenPanelMenuItem Text="Lista de Usuários" Icon="list" Path="usuarios" />
                        <RadzenPanelMenuItem Text="Novo Usuário" Icon="person_add" Path="usuario/novo" />
                        <RadzenPanelMenuItem Text="Auditoria de Usuários" Icon="history" Path="/auditoria?entidade=Usuario" />
                    </RadzenPanelMenuItem>
                </Authorized>
            </AuthorizeView>

            <AuthorizeView Roles="Administrador,SA" Context="adminContextAud">
                <Authorized>
                    <RadzenPanelMenuItem Text="Auditoria Geral" Icon="history" Path="/auditoria" />
                </Authorized>
            </AuthorizeView>

            <RadzenPanelMenuItem Text="Logout" Icon="logout">
                <Template>
                    <form method="post" action="/auth/logout" style="margin: 0;">
                        <button type="submit" class="rz-button rz-button-text" style="width:100%; text-align:left;">Logout</button>
                    </form>
                </Template>
            </RadzenPanelMenuItem>
        </RadzenPanelMenu>
    </Authorized>
</AuthorizeView>

@code {
    private string? currentUrl;
    private string? nomeUsuario;
    private PersonalizacaoDto? personalizacao;

    protected override async Task OnInitializedAsync()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;

        personalizacao = await PersonalizacaoApiService.ObterAsync();

        var usuario = await UsuarioLogadoService.ObterUsuarioAtualAsync();
        nomeUsuario = usuario?.Nome ?? "Usuário";
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}